// Inventory/theme/index.ts

import { colors } from './colors';
import { typography, textStyles, fontWeights } from './typography';
import { spacing, borderRadius, shadows } from './spacing';
import { usePoppinsFonts, fonts } from './fonts';

export const theme = {
  colors,
  typography,
  textStyles,
  fontWeights,
  spacing,
  borderRadius,
  shadows,
};

export { 
  colors, 
  typography, 
  textStyles, 
  fontWeights,
  spacing, 
  borderRadius, 
  shadows,
  usePoppinsFonts,
  fonts,
};

export default theme;
